{
  "name": "mso-runtime",
  "version": "0.1.1",
  "private": true,
  "description": "Deterministic execution helpers for the Multi-Workstream Orchestrator skill.",
  "type": "module",
  "bin": {
    "mso-runtime": "./src/cli.js"
  },
  "scripts": {
    "test": "node --test",
    "start": "node ./src/cli.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "yaml": "^2.8.1"
  }
}
